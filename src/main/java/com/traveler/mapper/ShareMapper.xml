<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.traveler.mapper.ShareMapper">
    
    <select id="selectCount" resultType="int">
		select count(*) from TravelShareBoard;        
    </select>
    
    <select id="selectAll" resultType="com.traveler.mapper.ShareMapper">
        select boardNum,
        	   memberPicture,
        	   id,
        	   name,
        	   memberAge,
        	   memberTel,
        	   title,
        	   term,
        	   number,
        	   cost,
        	   content,
        	   picture,
        	   map
         from TravelShareBoard   
    </select>
    
    <select id="selectById" parameterType="int" resultType="com.traveler.model.Share">
        select boardNum,
        	   memberPicture,
        	   id,
        	   name,
        	   memberAge,
        	   memberTel,
        	   title,
        	   term,
        	   number,
        	   cost,
        	   content,
        	   picture,
        	   map
         from TravelShareBoard  
		 where id = #{id}
    </select>
    
        <select id="selectPage" 
        	parameterType="com.traveler.util.Pagination" 
        	resultType="com.traveler.model.Share"
        	databaseId="mysql">
         select boardNum,
        	   memberPicture,
        	   id,
        	   name,
        	   memberAge,
        	   memberTel,
        	   title,
        	   term,
        	   number,
        	   cost,
        	   content,
        	   picture,
        	   map
         from TravelShareBoard  
		 order by id desc
		 limit #{start}, #{length} 
    </select>
    
      <insert id="insert" 
        	parameterType="com.traveler.model.Share"
        	useGeneratedKeys="true"
        	keyProperty="id"
        	databaseId="mysql">
        
        insert into TravelShareBoard
		(
			   boardNum,
			   memberPicture,
        	   id,
        	   name,
        	   memberAge,
        	   memberTel,
        	   title,
        	   term,
        	   number,
        	   cost,
        	   content,
        	   picture,
        	   map
		)
		values
		(
		    #{boardNum,				jdbcType=NUMERIC},
			#{memberPicture, 		jdbcType=VARCHAR},
			#{id, 					jdbcType=VARCHAR},
			#{name, 				jdbcType=VARCHAR},
			#{memberAge, 			jdbcType=NUMERIC},
			#{memberTel, 			jdbcType=VARCHAR},
			#{title, 				jdbcType=VARCHAR},
			#{term, 				jdbcType=VARCHAR},
			#{number, 				jdbcType=NUMERIC},
			#{cost, 				jdbcType=NUMERIC},
			#{content, 				jdbcType=NUMERIC},
			#{picture, 				jdbcType=NUMERIC},
			#{map, 					jdbcType=NUMERIC}
		)
    </insert>
         
</mapper>

